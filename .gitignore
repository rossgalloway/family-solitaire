# See https://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
/node_modules

# testing
/coverage

# production
/build
/dist
*.tsbuildinfo

# Local family photos, source artwork, and design iterations. The production
# app uses only the optimized assets under src/assets/.
/artwork/

# Capacitor native projects (android/ is tracked; ios/ is not yet committed)
/ios
.DS_Store

# Android signing keys — NEVER commit these (CI uses repo secrets instead)
*.keystore
*.jks

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

TODO.txt
.superpowers/
docs/superpowers/

# Icon build intermediates — only the SVG sources in resources/ are committed
resources/*.png

# Unused PWA artifacts that capacitor-assets generates whether asked or not
/icons/
/public/manifest.webmanifest
